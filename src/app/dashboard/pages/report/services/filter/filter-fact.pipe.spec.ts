import { FilterFactPipe } from './filter-fact.pipe';

describe('FilterFactPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFactPipe();
    expect(pipe).toBeTruthy();
  });
});
