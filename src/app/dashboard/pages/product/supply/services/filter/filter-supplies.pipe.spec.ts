import { FilterSuppliesPipe } from './filter-supplies.pipe';

describe('FilterSuppliesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSuppliesPipe();
    expect(pipe).toBeTruthy();
  });
});
